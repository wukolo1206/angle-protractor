---
project: 量角器與角度（四上第三單元）
category: 學科工具集
status: 開發中
version: "v1.0 四頁完成，本機測試通過，尚未部署"
url: "—"
next_action: 建 GitHub repo 並開 Pages；iPad 實機試用調整對齊容差（中心 3%、角度 1°）
updated: 2026-09-13
---

# CLAUDE.md — 量角器與角度

康軒數學**四上第三單元「角度」**的互動教具。純 HTML + CSS + 原生 JS，無框架、無 build。

**設計依據**：`../docs/superpowers/specs/2026-09-13-u3-angle-design.md`（v1.2，一輪交叉審查＋實作期修正）
**備課分析**：`../docs/superpowers/specs/2026-09-13-u3-angle-analysis.md`（v1.1）

## 頁面（照課本活動分頁，使用者裁決）

| 檔案 | 內容 | 課本 |
|---|---|---|
| `index.html` | 導覽＋待做實作卡計數＋給老師說明 | — |
| `act1.html` | 開門活動＋活動一 量角器的認識與報讀 | 印 p31–35 |
| `act2.html` | 活動二 測量角的大小和畫角 | 印 p36–40 |
| `act3.html` | 活動三 旋轉角（含金庫密碼） | 印 p41–43、p46 第2題 |
| `act4.html` | 活動四 角的合成與分解 | 印 p44–45、p46 第3題 |

每頁三分頁：🔍探索（教師觸控大屏）／✏️練習（學生 iPad，關卡標課本題號）／🏆挑戰。
課本要用實體量角器、三角板、附件的題目放**課堂實作卡**，有「我做完了」「稍後在課堂做」兩個按鈕。

頁碼換算：課本印刷頁＝PDF頁＋30；習作印刷頁＝PDF頁＋27；教冊印刷頁＝PDF頁－2。

## 檔案分工

| 檔案 | 用途 |
|---|---|
| `angle-core.js` | 核心教學邏輯與診斷，**網頁與測試共用同一份，不可搬回 HTML 內嵌** |
| `protractor.js` | SVG 繪圖（量角器兩款、鐘面、射線、扇形）與拖曳 |
| `ui.js` | 分頁、回饋、關卡、課堂實作卡、localStorage |
| `style.css` | 共用樣式 |
| `img/` | 考古題原圖（從 `../past-exam-trainer/img/` 複製） |

## 核心邏輯不能亂改的地方（改了要重跑 `python run-tests.py`）

1. **角度座標**：θ＝射線與量角器底線向右的夾角，逆時針為正。L 款（課本款，外圈 0 在左）內圈＝θ；R 款外圈＝θ。
2. **診斷不用判定順序裁決**：先收集所有成立條件，多於一個回 `AMBIGUOUS`。可用題中 `AMBIGUOUS` 必須是 0（窮舉斷言）。
3. **消歧義在出題端**：始邊在 0 的題正解不出 81～99；非零起點題單邊讀數與正解相差須 ≥ 10；跨圈差等於正解不算歧義。
4. **課本做做看① 印p35（外圈 70～135）只能放三格相減形式**：單一答案形式答 70 是真歧義。
5. **鐘面格數**：n＝6 不判「另一邊」、n＝12 答 0 歸 `ZERO_FULL_TURN`。方向、格數、度數三格各自判定。
6. **合成分解**：運算、總量、答案三格各自判定。

## 教學內容的規矩

- **回饋語不說「讀內圈／讀外圈」**，說「角的一邊壓在哪一圈的 0」（教冊注意事項 1：學生的量角器內外圈可能和課本相反）。e2e 會檢查。
- 讀錯圈先問「**這個角比直角（90°）大還是小？**」（教冊注意事項 2）。
- 量角器有 L／R 兩款，**刻度字只旋轉不鏡像**。活動一問 6 以後兩款隨機。
- 含方向或圈別的回饋（竹籤題）**由參數組字**，不可寫死「外圈、順時針」。
- 課本、習作、教冊、考古題的數值都已對原圖核對，改動前要重新看原圖。

## 能宣稱什麼、不能宣稱什麼

- **有教冊明文依據的學生困難**：讀錯圈（70°↔110°）、角度保留（擺放方向）——教冊 PDF p36（印 p34）注意事項 2、3
- **其他全部只是課本內容的數位化練習**：對齊與延長（指導重點 3 是教學提醒）、非零起點（誘答）、旋轉角（誘答＋教學提醒）、合成分解
- 點數大格、分階段解鎖、中心吸附是**固定式協助**，**不叫鷹架**
- **網頁量角器不等於真量角器**，不可拿網頁成績推論學生會操作實體量角器

## 測試

```bash
python run-tests.py
```
- `angle-core.test.js`：課本／習作／教冊標準答案＋窮舉斷言
- `e2e.spec.py`：五頁端到端（自動起 http server）
- **不能用 `file://` 跑 e2e**

## 不能動的地方

- `angle-core.js` 的診斷代碼名稱：UI 回饋對照表以代碼為 key，改名要同步四頁
- `img/` 考古題原圖檔名：挑戰分頁寫死路徑
- 課本題目的角度數值：已逐題對原圖，改動需重新核對

## 部署後驗證清單

- `python run-tests.py` 兩層全過
- 開 `https://wukolo1206.github.io/angle-protractor/` → 四張卡片連結正常
- `act1.html` 練習「40 還是 140」答 40 → 回饋問「比直角（90°）大還是小」
- `act2.html` 練習「自己量」：拖量角器靠近頂點會吸附，轉到對齊才能填答
- `act3.html` 練習「從 12 順時針」第 4 題答 0 格 → 回饋「一整圈」
- **iPad 實機**：拖量角器、轉圓環、拖竹籤手感；對齊容差是否太嚴
